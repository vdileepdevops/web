import { AlphanumericcharsonlyDirective } from './alphanumericcharsonly.directive';

describe('AlphanumericcharsonlyDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphanumericcharsonlyDirective();
    expect(directive).toBeTruthy();
  });
});
