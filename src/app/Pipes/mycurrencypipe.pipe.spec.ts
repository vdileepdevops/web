import { MycurrencypipePipe } from './mycurrencypipe.pipe';

describe('MycurrencypipePipe', () => {
  it('create an instance', () => {
    const pipe = new MycurrencypipePipe();
    expect(pipe).toBeTruthy();
  });
});
